'use strict';

import Q from 'q';
import utils from '../utils';
import Auto_objects from './objects.auto.js';

/**
 * Contiene los metodos necesarios para trabajar
 * con la tabla.
 */
export default class Objects extends Auto_objects{
  constructor(owner){
    super(owner);
  }
}